/* tslint:disable */
/* eslint-disable */
/**
 * Search API
 * With UiTdatabank\'s Search API you can search events, places and organizers.
 *
 * The version of the OpenAPI document: 3.0
 * Contact: technical-support@publiq.be
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { CommonFacet } from './CommonFacet';
import {
    CommonFacetFromJSON,
    CommonFacetFromJSONTyped,
    CommonFacetToJSON,
    CommonFacetToJSONTyped,
} from './CommonFacet';

/**
 * Facet counts per possible filter & value.
 * @export
 * @interface GetEvents200ResponseFacet
 */
export interface GetEvents200ResponseFacet {
    /**
     * Possible values for a filter, with an estimated total count if the filter is applied with one of the given values.
     * 
     * For example a list of facets for the `regions` filter can be:
     * 
     * ```json
     * {
     *   "regions": {
     *     "prv-vlaams-brabant": {
     *       "name": {
     *         "nl": "Vlaams-Brabant",
     *         "fr": "Brabant Flamand",
     *         "en": "Flemish Brabant"
     *       },
     *       "count": 22,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     },
     *     "prv-antwerpen": {
     *       "name": {
     *         "nl": "Antwerpen",
     *         "fr": "Anvers",
     *         "en": "Antwerp"
     *       },
     *       "count": 18,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     },
     *     "prv-limburg": {
     *       "name": {
     *         "nl": "Limburg",
     *         "fr": "Limbourg",
     *         "en": "Limburg"
     *       },
     *       "count": 11,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     }
     *   }
     * }
     * ```
     * 
     * If the user then selects "Vlaams-Brabant (22)" to see the 22 results in Vlaams-Brabant, the `regions` filter should be applied with the value `prv-vlaams-brabant`.
     * @type {{ [key: string]: CommonFacet; }}
     * @memberof GetEvents200ResponseFacet
     */
    regions?: { [key: string]: CommonFacet; };
    /**
     * Possible values for a filter, with an estimated total count if the filter is applied with one of the given values.
     * 
     * For example a list of facets for the `regions` filter can be:
     * 
     * ```json
     * {
     *   "regions": {
     *     "prv-vlaams-brabant": {
     *       "name": {
     *         "nl": "Vlaams-Brabant",
     *         "fr": "Brabant Flamand",
     *         "en": "Flemish Brabant"
     *       },
     *       "count": 22,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     },
     *     "prv-antwerpen": {
     *       "name": {
     *         "nl": "Antwerpen",
     *         "fr": "Anvers",
     *         "en": "Antwerp"
     *       },
     *       "count": 18,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     },
     *     "prv-limburg": {
     *       "name": {
     *         "nl": "Limburg",
     *         "fr": "Limbourg",
     *         "en": "Limburg"
     *       },
     *       "count": 11,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     }
     *   }
     * }
     * ```
     * 
     * If the user then selects "Vlaams-Brabant (22)" to see the 22 results in Vlaams-Brabant, the `regions` filter should be applied with the value `prv-vlaams-brabant`.
     * @type {{ [key: string]: CommonFacet; }}
     * @memberof GetEvents200ResponseFacet
     */
    types?: { [key: string]: CommonFacet; };
    /**
     * Possible values for a filter, with an estimated total count if the filter is applied with one of the given values.
     * 
     * For example a list of facets for the `regions` filter can be:
     * 
     * ```json
     * {
     *   "regions": {
     *     "prv-vlaams-brabant": {
     *       "name": {
     *         "nl": "Vlaams-Brabant",
     *         "fr": "Brabant Flamand",
     *         "en": "Flemish Brabant"
     *       },
     *       "count": 22,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     },
     *     "prv-antwerpen": {
     *       "name": {
     *         "nl": "Antwerpen",
     *         "fr": "Anvers",
     *         "en": "Antwerp"
     *       },
     *       "count": 18,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     },
     *     "prv-limburg": {
     *       "name": {
     *         "nl": "Limburg",
     *         "fr": "Limbourg",
     *         "en": "Limburg"
     *       },
     *       "count": 11,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     }
     *   }
     * }
     * ```
     * 
     * If the user then selects "Vlaams-Brabant (22)" to see the 22 results in Vlaams-Brabant, the `regions` filter should be applied with the value `prv-vlaams-brabant`.
     * @type {{ [key: string]: CommonFacet; }}
     * @memberof GetEvents200ResponseFacet
     */
    themes?: { [key: string]: CommonFacet; };
    /**
     * Possible values for a filter, with an estimated total count if the filter is applied with one of the given values.
     * 
     * For example a list of facets for the `regions` filter can be:
     * 
     * ```json
     * {
     *   "regions": {
     *     "prv-vlaams-brabant": {
     *       "name": {
     *         "nl": "Vlaams-Brabant",
     *         "fr": "Brabant Flamand",
     *         "en": "Flemish Brabant"
     *       },
     *       "count": 22,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     },
     *     "prv-antwerpen": {
     *       "name": {
     *         "nl": "Antwerpen",
     *         "fr": "Anvers",
     *         "en": "Antwerp"
     *       },
     *       "count": 18,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     },
     *     "prv-limburg": {
     *       "name": {
     *         "nl": "Limburg",
     *         "fr": "Limbourg",
     *         "en": "Limburg"
     *       },
     *       "count": 11,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     }
     *   }
     * }
     * ```
     * 
     * If the user then selects "Vlaams-Brabant (22)" to see the 22 results in Vlaams-Brabant, the `regions` filter should be applied with the value `prv-vlaams-brabant`.
     * @type {{ [key: string]: CommonFacet; }}
     * @memberof GetEvents200ResponseFacet
     */
    facilities?: { [key: string]: CommonFacet; };
    /**
     * Possible values for a filter, with an estimated total count if the filter is applied with one of the given values.
     * 
     * For example a list of facets for the `regions` filter can be:
     * 
     * ```json
     * {
     *   "regions": {
     *     "prv-vlaams-brabant": {
     *       "name": {
     *         "nl": "Vlaams-Brabant",
     *         "fr": "Brabant Flamand",
     *         "en": "Flemish Brabant"
     *       },
     *       "count": 22,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     },
     *     "prv-antwerpen": {
     *       "name": {
     *         "nl": "Antwerpen",
     *         "fr": "Anvers",
     *         "en": "Antwerp"
     *       },
     *       "count": 18,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     },
     *     "prv-limburg": {
     *       "name": {
     *         "nl": "Limburg",
     *         "fr": "Limbourg",
     *         "en": "Limburg"
     *       },
     *       "count": 11,
     *       "children": {
     *         ... // a nested list of facets
     *       }
     *     }
     *   }
     * }
     * ```
     * 
     * If the user then selects "Vlaams-Brabant (22)" to see the 22 results in Vlaams-Brabant, the `regions` filter should be applied with the value `prv-vlaams-brabant`.
     * @type {{ [key: string]: CommonFacet; }}
     * @memberof GetEvents200ResponseFacet
     */
    labels?: { [key: string]: CommonFacet; };
}

/**
 * Check if a given object implements the GetEvents200ResponseFacet interface.
 */
export function instanceOfGetEvents200ResponseFacet(value: object): value is GetEvents200ResponseFacet {
    return true;
}

export function GetEvents200ResponseFacetFromJSON(json: any): GetEvents200ResponseFacet {
    return GetEvents200ResponseFacetFromJSONTyped(json, false);
}

export function GetEvents200ResponseFacetFromJSONTyped(json: any, ignoreDiscriminator: boolean): GetEvents200ResponseFacet {
    if (json == null) {
        return json;
    }
    return {
        
        'regions': json['regions'] == null ? undefined : (mapValues(json['regions'], CommonFacetFromJSON)),
        'types': json['types'] == null ? undefined : (mapValues(json['types'], CommonFacetFromJSON)),
        'themes': json['themes'] == null ? undefined : (mapValues(json['themes'], CommonFacetFromJSON)),
        'facilities': json['facilities'] == null ? undefined : (mapValues(json['facilities'], CommonFacetFromJSON)),
        'labels': json['labels'] == null ? undefined : (mapValues(json['labels'], CommonFacetFromJSON)),
    };
}

export function GetEvents200ResponseFacetToJSON(json: any): GetEvents200ResponseFacet {
    return GetEvents200ResponseFacetToJSONTyped(json, false);
}

export function GetEvents200ResponseFacetToJSONTyped(value?: GetEvents200ResponseFacet | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'regions': value['regions'] == null ? undefined : (mapValues(value['regions'], CommonFacetToJSON)),
        'types': value['types'] == null ? undefined : (mapValues(value['types'], CommonFacetToJSON)),
        'themes': value['themes'] == null ? undefined : (mapValues(value['themes'], CommonFacetToJSON)),
        'facilities': value['facilities'] == null ? undefined : (mapValues(value['facilities'], CommonFacetToJSON)),
        'labels': value['labels'] == null ? undefined : (mapValues(value['labels'], CommonFacetToJSON)),
    };
}

