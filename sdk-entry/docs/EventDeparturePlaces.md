
# EventDeparturePlaces

A list of URIs representing departure places for this event. Each URI must reference an existing place in UiTdatabank. Only applicable when the event\'s audienceType is childrenOnly.

## Properties

Name | Type
------------ | -------------

## Example

```typescript
import type { EventDeparturePlaces } from ''

// TODO: Update the object below with actual values
const example = {
} satisfies EventDeparturePlaces

console.log(example)

// Convert the instance to a JSON string
const exampleJSON: string = JSON.stringify(example)
console.log(exampleJSON)

// Parse the JSON string back to an object
const exampleParsed = JSON.parse(exampleJSON) as EventDeparturePlaces
console.log(exampleParsed)
```

[[Back to top]](#) [[Back to API list]](../README.md#api-endpoints) [[Back to Model list]](../README.md#models) [[Back to README]](../README.md)


